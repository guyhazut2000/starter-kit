# Prisma Seed Data

This folder holds all data used by `prisma/seed.ts`.

- **`users.ts`** – Seed users (name, email, password). Loaded by the seed script.

Add more files as needed (e.g. `sessions.ts`, `products.ts`). Import them in `prisma/seed.ts`.
